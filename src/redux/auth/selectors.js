export const user = (state) => state.auth.isLogged;
export const token = (state) => state.auth.isLogged;
export const isRefresh = (state) => state.auth.isLogged;
export const isLogged = (state) => state.auth.isLogged;
export const errorAccount = (state) => state.auth.error;
